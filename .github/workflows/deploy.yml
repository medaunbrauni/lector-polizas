name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 62.151.183.45
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: /var/www/vhosts/movi.digital/lector.movi.digital/app/deploy.sh
